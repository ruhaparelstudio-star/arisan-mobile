# File ini sudah digantikan oleh:
#   .env.development.example  → untuk local development
#   .env.production.example   → untuk production build
#
# Cara setup:
#   cp .env.development.example .env.development   (lalu isi IP LAN kamu)
#   cp .env.production.example .env.production     (lalu isi nilai production)
